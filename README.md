# Εργασία Ειδικά Θέματα Τεχνολογίας Λογισμικού

- `frontend/`: React app (UI)
- `backend/`: Node.js API για σύνδεση με MariaDB και Sequelize για ORM

`cd frontend && npm install && npm start`
`cd backend && npm install && node index.js`
